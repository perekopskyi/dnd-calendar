import { ROUTES } from '../../App'
import { Button } from '../../components/common/Button'

export const Labels = () => {
  return (
    <div>
      <Button to={ROUTES.MAIN}>Back</Button>
      <h2>Labels</h2>
    </div>
  )
}
